/* variables in JavaScript:
const is a variable that cannot be changed
let is a variable that can be changed
var is an older way to declare variables, but it's less commonly used now due to its function scope behavior.
*/